server:
 context-path: /spring-boot
 port: 80
 
 
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_02?useSSL=false
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true